---
  - hosts: all
    become: true
    tasks:
      - name: Dump Morpheus Dict Full
        debug:
          msg: "{{ morpheus }}"
        ignore_errors: true
      - name: Dump Morpheus Dict Instance
        debug:
          msg: "{{ morpheus['instance'] }}"
        ignore_errors: true